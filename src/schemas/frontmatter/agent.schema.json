{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "agent-frontmatter",
  "title": "Claude Code sub-agent frontmatter",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "description"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Unique identifier using lowercase letters and hyphens."
    },
    "description": {
      "type": "string",
      "description": "When Claude should delegate to this subagent."
    },
    "tools": {
      "anyOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "Tools the subagent can use. Inherits all tools if omitted."
    },
    "disallowedTools": {
      "anyOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "Tools to deny, removed from inherited or specified list."
    },
    "model": {
      "type": "string",
      "description": "Model to use: 'sonnet', 'opus', 'haiku', a full model ID, or 'inherit'. Defaults to 'inherit'."
    },
    "permissionMode": {
      "type": "string",
      "enum": [
        "default",
        "acceptEdits",
        "auto",
        "dontAsk",
        "bypassPermissions",
        "plan"
      ],
      "description": "Permission mode for this subagent."
    },
    "maxTurns": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of agentic turns before the subagent stops."
    },
    "hooks": {
      "type": "object",
      "description": "Lifecycle hooks scoped to this subagent."
    },
    "memory": {
      "type": "string",
      "enum": ["user", "project", "local"],
      "description": "Persistent memory scope. Enables cross-session learning."
    },
    "background": {
      "type": "boolean",
      "description": "Set to true to always run this subagent as a background task. Default: false."
    },
    "effort": {
      "type": "string",
      "enum": ["low", "medium", "high", "xhigh", "max"],
      "description": "Effort level when this subagent is active. Overrides session effort level."
    },
    "isolation": {
      "type": "string",
      "enum": ["worktree"],
      "description": "Set to 'worktree' to run the subagent in a temporary git worktree."
    },
    "color": {
      "type": "string",
      "enum": [
        "red",
        "blue",
        "green",
        "yellow",
        "purple",
        "orange",
        "pink",
        "cyan"
      ],
      "description": "Display color for the subagent in the task list and transcript."
    },
    "initialPrompt": {
      "type": "string",
      "description": "Auto-submitted as the first user turn when this agent runs as the main session agent."
    }
  }
}
